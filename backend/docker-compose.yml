services:
  backend:
    depends_on: test_db

  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: testpassword
    # volumes:
    #   - ./db-data:/var/lib/postgresql/data
    ports: 8080:5432
    command: echo hello world
